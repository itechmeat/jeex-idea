# JEEX Idea PostgreSQL Host-Based Authentication Configuration
# This file controls client authentication for PostgreSQL

# TYPE  DATABASE        USER            ADDRESS                 METHOD

# "local" is for Unix domain socket connections only
local   all             all                                     md5

# IPv4 local connections:
host    all             all             127.0.0.1/32            md5

# IPv6 local connections:
host    all             all             ::1/128                 md5

# Allow replication connections from localhost, by a user with the
# replication privilege.
local   replication     all                                     trust
host    replication     all             127.0.0.1/32            md5
host    replication     all             ::1/128                 md5

# Docker network connections (internal network access)
# Specific to jeex-data network subnet
host    all             all             172.19.0.0/16           md5